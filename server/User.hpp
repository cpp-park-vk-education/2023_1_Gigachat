#pragma once

#include <string>

class User {
public:
    // TODO: getters, setters

private:
    std::string _login;
    std::string _password;
    std::string _nickName;
    std::string _language;
};
